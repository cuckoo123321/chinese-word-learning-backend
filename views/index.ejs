<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('template/head') %>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        p {
            font-size: 50px;
            margin-bottom: 20px;
            color: rgb(56 137 183);
            margin: 0 0 100px 0;
        }

        .btn-container {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }
        p, .btn-container{
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            top: 25vh;
        }

        .btn {
            flex: 1;
            max-width: 100px;
        }

        @media (max-width: 768px) {
            p, .btn-container {
                top: 10vh;
            }
            .btn {
                flex: 0 0 100%;
            }
        }
    </style>
</head>
<body>
    <%- include('template/navbar') %> 
    <div class="container">
        <% if(admin_name) { %>            
            <div><p>歡迎 <%= admin_full_name %> 使用本管理系統</p> </div>
            <div class="btn-container">
                <a class="btn btn-primary" href="<%= baseUrl %>/adminList">系統管理</a>
                <a class="btn btn-primary" href="<%= baseUrl %>/carouselList">首頁輪播</a>  
                <a class="btn btn-primary" href="<%= baseUrl %>/articleList">學術成果</a>              
                <a class="btn btn-primary" href="<%= baseUrl %>/productList">出版產品</a>                
                <a class="btn btn-primary" href="<%= baseUrl %>/userList">網站會員</a>
            </div>
        <% } else { %>
            <p>歡迎使用「記憶術提升漢字詞學習」管理系統</p>
            <p>請系統管理員登入系統</p>  
        <% } %>  
    </div>
</body>
</html>

