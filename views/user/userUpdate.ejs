<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../template/head') %>
    <%- include('../../css/addUpdateStyle') %>
</head>
<body>
    <%- include('../template/navbar') %>
    <div class="container mt-5">
        <% if(admin_name) { %>
        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6">
                <h2>編輯會員</h2>
                <% if(errorMessage && errorMessage.length > 0){ %>
                    <div class="alert alert-dismissible alert-danger">
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        <strong><%= errorMessage %></strong>
                    </div>
                <% } %>

                <form method="POST" action="<%= baseUrl %>/update_user/<%= user.user_id %>" data-bitwarden-watching="1">
                    
                    <div class="form-group list">
                        <label class="form-label">帳號：</label>
                        <input name="user_name" type="text" class="form-control" value="<%= user.user_name %>"  placeholder="請輸入帳號" required="required">
                    </div>

                    <div class="form-group list">
                        <label class="form-label">密碼：</label>
                        <input name="user_password" type="password" class="form-control" value="<%= user.user_password %>"  placeholder="請輸入密碼" required="required">
                    </div>

                    <div class="form-group list_radio">
                        <label class="form-label">狀態：</label>
                        <div class="form-check div_radio">
                            <input class="form-check-input input_radio" type="radio" name="user_disabled" value="0" id="enabled" <% if (user.user_disabled === 0) { %>checked<% } %>>
                            <label class="form-check-label" for="enabled">
                                啟用
                            </label>
                        </div>
                        <div class="form-check div_radio">
                            <input class="form-check-input input_radio" type="radio" name="user_disabled" value="1" id="disabled" <% if (user.user_disabled === 1) { %>checked<% } %>>
                            <label class="form-check-label" for="disabled">
                                停權
                            </label>
                        </div>
                    </div>

                    
                    <div class="form-group list mt-4">
                        <input type="submit" class="btn btn-success btn_margin" onclick="return confirm('確認更新資料？');"/>
                        <a class="btn btn-secondary btn_margin" href="<%= baseUrl %>/userList">取消</a>
                    </div>
                </form>
            </div>
        <div class="col-md-3"></div>
    </div>
    <% } else { %>
        <div class="loginContainer">
            <p class="loginText">歡迎使用「記憶術提升漢字詞學習」管理系統</p>
            <p class="loginText">請系統管理員登入系統</p>  
        </div>
    <% } %>
</div>

    <script>
     
    </script>
</body>
</html>
